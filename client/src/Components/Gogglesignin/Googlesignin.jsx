// import React from 'react';
// import { GoogleLogin } from 'react-google-login';

// const GoogleSignInButton = ({ onSuccess, onFailure }) => {
//   const googleClientId = '879986446151-dm25g8gcheblclds5lkemlp92ljgef54.apps.googleusercontent.com';

//   const handleGoogleSignInSuccess = (response) => {
//     // Call parent component's onSuccess callback with Google response
//     onSuccess(response);
//   };

//   const handleGoogleSignInFailure = (error) => {
//     // Call parent component's onFailure callback with error
//     onFailure(error);
//   };

//   return (
//     <GoogleLogin
//       clientId={googleClientId}
//       buttonText="Sign up with Google"
//       onSuccess={handleGoogleSignInSuccess}
//       onFailure={handleGoogleSignInFailure}
//       cookiePolicy={'single_host_origin'}
//     />
//   );
// };

// export default GoogleSignInButton;
